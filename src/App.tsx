import React from "react";
import Login from "./components/Login"; // Adjust the path as needed

const App: React.FC = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default App;
